#!/usr/bin/env ruby
require_relative '../lib/turn'
 
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
 
puts "Welcome to Tic Tac Toe!"

move(board, 4, "X")
move(board, 0, "O")

display_board(board)

turn(board)


# user_input = gets.strip
#turn(user_input)
#input_to_index(user_input)
#move(board, index, "X")

#display_board(board)


